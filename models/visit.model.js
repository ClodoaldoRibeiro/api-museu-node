export class Visit {
  constructor({ id, visitorId, date, purpose, notes }) {
    this.id = id
    this.visitorId = visitorId
    this.date = date
    this.purpose = purpose
    this.notes = notes
  }
}
